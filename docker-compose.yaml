version: "3.9"
services:
  ansible:
    build: 
      context: .
      dockerfile: ansible.Dockerfile
    expose:
      - "22"
  # web:
  #   build:
  #     contex
  mongo:
    build: 
      context: .
      dockerfile: centos.Dockerfile 
    expose: 
      - "22"

  mysql:
    build:
      context: .
      dockerfile: alpine.Dockerfile
    expose:
      - "22"